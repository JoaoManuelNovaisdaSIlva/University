#include <string.h>
#include <stdio.h>

int main (char *a, char *b) // ou a[], b[]
{
	int t = -1, tamanho;

	// comparar tamanho das strings
	// ver se a string b esta ,por completo, dentro da string a
	// t = posição onde b começa em a

	strlen(a) = tamanho;
	printf("%d",tamanho);
}